spring:
  application:
    name: bootguard
  threads:
    virtual:
      enabled: true
  datasource:
    url: jdbc:mariadb://localhost:3306/bootguard
    username: bootguard
    password: bootguard
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
vaadin:
  launch-browser: true
